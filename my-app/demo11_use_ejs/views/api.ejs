<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .page-message { color: blue }
      </style>

</head>

<body>
    This is part of the body.
    <h1>姓名：<%= name %> 年龄：<%= age %>
        <h1>姓名：<%= name_local %> 年龄：<%= age_local %>
    </h1>

    <h1 class="page-message"><%= message %></h1>
    <h1>家庭首页homeList</h1>
    <table>
        <tr>
            <td>ID</td>
            <td>名称</td>
        </tr>
        <% for (let i=0; i<homeList.length; i++) { %>

        <tr>
            <td><%= homeList[i].id %></td>
            <td><%= homeList[i].name %></td>
        </tr>

        <% } %>
    </table>

    <h1>家庭首页homeList_local</h1>
    <table>
        <tr>
            <td>ID</td>
            <td>名称</td>
        </tr>
        <% for (let i=0; i<homeList_local.length; i++) { %>

        <tr>
            <td><%= homeList_local[i].id %></td>
            <td><%= homeList_local[i].name %></td>
        </tr>

        <% } %>
    </table>
</body>

</html>